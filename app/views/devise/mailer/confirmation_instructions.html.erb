<p><%= I18n.t('devise.mailer.confirmation_instructions.greeting', recipient: @email) %></p>

<p><%= I18n.t('devise.mailer.confirmation_instructions.subject') %></p>

<p><%= link_to I18n.t('devise.mailer.confirmation_instructions.action'), "#{Rails.application.credentials[:fe_url]}/#{locale}/confirmation?confirmation_token=#{@token}" %></p>
